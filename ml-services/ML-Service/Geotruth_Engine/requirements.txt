# GeoTruth — Final Production Dependencies
# Install: pip install -r requirements.txt

# ── API Server ──────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0

# ── HTTP Client (For Open-Meteo API) ────────────────────────────
httpx>=0.27.0

# ── Machine Learning (The Master & Inertial Engines) ────────────
xgboost>=2.0.3
scikit-learn>=1.4.2
numpy>=1.26.4
pandas>=2.2.2
joblib>=1.4.2

# ── Audio Processing (The Acoustic Engine) ──────────────────────
# tflite-runtime is the lightweight version of TensorFlow. 
# If it fails to install on Windows, comment it out and uncomment tensorflow.
tflite-runtime>=2.14.0
# tensorflow>=2.16.0

# ── Development & Testing ───────────────────────────────────────
pytest>=8.2.0
pytest-asyncio>=0.23.7